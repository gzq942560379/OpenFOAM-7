CPP        = cpp -traditional-cpp $(GFLAGS)
LD         = swld
AR         = swar
ARFLAGS    = cr
RANLIB     = swranlib

# PROJECT_LIBS = -l$(WM_PROJECT) -ldl
GLIBS      =  -lz -lm -lm_slave

include $(GENERAL_RULES)/standard

include $(DEFAULT_RULES)/c
include $(DEFAULT_RULES)/c++